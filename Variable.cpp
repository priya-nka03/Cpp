#include <iostream>
using namespace std;

int main(){
    // Varaible => it is like a container inside which we can put data. This data can be characters,numbers,float etc
    // We can store data inside it of specified type only.
    // syntax:  datatype vairable_name = data;

    int a=90;
    cout<<a<<endl;
    char b='j';
    string st="string";
    float f=89.4;
    cout<<b<<endl<<st<<endl<<f<<endl;
}